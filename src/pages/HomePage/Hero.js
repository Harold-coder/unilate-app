import React from "react"

export default function Hero() {
    return (
        <h1 className="hero">
            <span className="hero-letter" id="first-letter" style={{animationDelay: '0s'}}>U</span>
            <span className="hero-letter" style={{animationDelay: '0.1s'}}>n</span>
            <span className="hero-letter" style={{animationDelay: '0.2s'}}>i</span>
            <span className="hero-letter" style={{animationDelay: '0.3s'}}>l</span>
            <span className="hero-letter" style={{animationDelay: '0.4s'}}>a</span>
            <span className="hero-letter" style={{animationDelay: '0.5s'}}>t</span>
            <span className="hero-letter" style={{animationDelay: '0.6s'}}>e</span>
        </h1>
    )
}
